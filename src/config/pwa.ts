// =============================================
// FuelUp - PWA constants (client diagnostics + install)
// Service-worker cache names live ONLY in public/sw.js (single source).
// This module holds app-side PWA metadata, never cache contents.
// =============================================

export const PWA_VERSION = '1.0.0';
export const SW_PATH = '/sw.js';
export const SW_SCOPE = '/';
export const BG_SYNC_TAG = 'fuelup-sync';
