// Analytics boundary: transforms stored domain data into trends,
// summaries, and chart/heatmap view models. Consumes domain data;
// never mutates unrelated stores.
export * from '@/lib/calculations/analytics';
